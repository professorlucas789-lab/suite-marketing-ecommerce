{
  "scripts": {
    "build": "npm --prefix precocerto install --force --legacy-peer-deps && npm --prefix precocerto run build"
  },
  "devDependencies": {
    "firebase-admin": "^14.4.0",
    "playwright": "^1.62.1"
  }
}
